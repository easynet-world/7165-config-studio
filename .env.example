# Config Studio Configuration

# Server Settings
# Server port number (type:number, min:1, max:65535)
PORT=8880
# Server host address (default: localhost)
HOST=localhost
# Node.js environment (enum:development|production|test)
NODE_ENV=development

# Data Directory
# Custom data directory path for systems registry (optional, relative or absolute path)
# If not set, defaults to ./data/
# DATA_DIR=./data

# Logging
# Log level (enum:debug|info|warn|error)
LOG_LEVEL=info
# Enable file watching for real-time updates (enum:true|false)
FILE_WATCHER_ENABLED=true
# WebSocket server enabled (enum:true|false)
WEBSOCKET_ENABLED=true
