# Frontend Application Configuration

# Application
# Application name (required)
APP_NAME="My Awesome App"
# Application version (required, pattern:^\d+\.\d+\.\d+$)
APP_VERSION=1.0.0
# Application environment (enum:development|staging|production)
APP_ENV=production

# API
# API base URL (required, type:url)
API_BASE_URL=https://api.example.com
# API request timeout in milliseconds (type:number, min:1000)
API_TIMEOUT=10000
# API retry attempts on failure (type:number, min:0, max:10)
API_RETRY_ATTEMPTS=3

# Feature Flags
# Enable new UI features (enum:true|false)
FEATURE_NEW_UI=true
# Enable dark mode (enum:true|false)
FEATURE_DARK_MODE=false
# Enable analytics tracking (enum:true|false)
FEATURE_ANALYTICS=true

# Build
# Build target platform (enum:browser|node|electron)
BUILD_TARGET=browser
# Build mode (enum:development|production)
BUILD_MODE=production
# Public asset path (required)
PUBLIC_PATH=/

# Performance
# Enable lazy loading (enum:true|false)
LAZY_LOAD_ENABLED=true
# Enable code splitting (enum:true|false)
CODE_SPLITTING=true
# Cache strategy (enum:aggressive|moderate|none)
CACHE_STRATEGY=aggressive
